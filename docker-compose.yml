# Build the Astro site and serve it with nginx on http://localhost:13369
#
#   docker compose up -d --build
#
name: braindrop-web

services:
  web:
    build:
      context: .
      target: runtime
    image: braindrop-web
    ports:
      - "13369:80"
    restart: unless-stopped
